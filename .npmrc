registry=https://registry.npmjs.org/
network-timeout=300000
fetch-retries=5
fetch-retry-factor=2
# Some cases of ERR_INVALID_THIS on Vercel are fixed by ensuring we don't use node-fetch or other global fetch incorrectly 
# though this usually is handled by pnpm internally.
